<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>School Dashboard</title>
    <!-- Bootstrap CSS -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet" />
    <!-- Font Awesome for Icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <style>
        body, html {
            margin: 0;
            padding: 0;
            height: 100%;
            overflow: hidden;
        }
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f7f9fc;
        }
        .dashboard-container {
            display: flex;
            height: 100vh; /* Full height view */
        }
        .sidebar {
            width: 250px;
            background-color: #2a3f54;
            padding: 20px;
            color: #fff;
            display: flex;
            flex-direction: column;
        }
        .sidebar h3 {
            margin-bottom: 20px;
        }
        .sidebar a {
            color: #adb5bd;
            text-decoration: none;
            margin: 10px 0;
            display: block;
            padding: 10px;
            border-radius: 5px;
        }
        .sidebar a:hover {
            color: #fff;
            background-color: #495057;
        }
        .content {
            flex-grow: 1;
            padding: 20px;
            overflow-y: auto; /* Allows content scrolling if content is too large */
        }
        .content h2 {
            font-size: 2rem;
            margin-bottom: 20px;
        }
        .welcome-card {
            background-color: #fff;
            border: 1px solid #e3e6f0;
            border-radius: 10px;
            padding: 20px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            text-align: center;
        }
        .welcome-card h3 {
            margin-bottom: 15px;
            font-size: 1.5rem;
        }
        .feature-cards {
            display: flex;
            flex-wrap: wrap;
            justify-content: space-between;
            margin-top: 20px;
        }
        .feature-card {
            background-color: #fff;
            width: 30%;
            margin: 10px 0;
            padding: 20px;
            text-align: center;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }
        .feature-card i {
            font-size: 3rem;
            margin-bottom: 10px;
            color: #00d9f5;
        }
        .feature-card h4 {
            margin-bottom: 10px;
        }
        .btn-logout {
            background-color: #ff6b6b;
            color: #fff;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            cursor: pointer;
        }
        .btn-logout:hover {
            background-color: #fa5252;
        }
        /* Media query to handle responsiveness on smaller devices */
        @@media (max-width: 768px) {
            .feature-cards {
                flex-direction: column;
            }
            .feature-card {
                width: 100%;
            }
            .sidebar {
                width: 100%;
                height: auto;
            }
        }
    </style>
</head>
<body>
    <div class="dashboard-container">
        <!-- Sidebar -->
        <div class="sidebar">
            <h3>School Dashboard</h3>
            <a href="#"><i class="fas fa-home"></i> Home</a>
            <a href="#"><i class="fas fa-user-graduate"></i> Students</a>
            <a href="#"><i class="fas fa-chalkboard-teacher"></i> Courses</a>
            <a href="#"><i class="fas fa-calendar-alt"></i> Attendance</a>
            <a href="#"><i class="fas fa-book"></i> Grade Reports</a>
            <a href="#"><i class="fas fa-bullhorn"></i> Announcements</a>
            <a href="#"><i class="fas fa-cogs"></i> Settings</a>
            <a href="#"><i class="fas fa-sign-out-alt"></i> Logout</a>
        </div>
        
        <!-- Main Content -->
        <div class="content">
            <h2>Welcome to Your School Dashboard!</h2>
            <div class="welcome-card">
                <h3>Hello, [User's Name]!</h3>
                <p>Manage your school operations, view student details, and track academic performance here.</p>
                <button class="btn-logout" onclick="window.location.href='@Url.Action("Logout")'">
                    <i class="fas fa-sign-out-alt"></i> Logout
                </button>
            </div>

            <!-- Feature Cards -->
            <div class="feature-cards">
                <div class="feature-card">
                    <i class="fas fa-user-graduate"></i>
                    <h4>Manage Students</h4>
                    <p>View and update student information and performance records.</p>
                </div>
                <div class="feature-card">
                    <i class="fas fa-chalkboard-teacher"></i>
                    <h4>View Courses</h4>
                    <p>Check course schedules, materials, and assigned instructors.</p>
                </div>
                <div class="feature-card">
                    <i class="fas fa-calendar-alt"></i>
                    <h4>Track Attendance</h4>
                    <p>View and update student attendance records easily.</p>
                </div>
                <div class="feature-card">
                    <i class="fas fa-book"></i>
                    <h4>Grade Reports</h4>
                    <p>Access detailed grade reports for students in various courses.</p>
                </div>
                <div class="feature-card">
                    <i class="fas fa-bullhorn"></i>
                    <h4>Make Announcements</h4>
                    <p>Post important announcements for students and staff.</p>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS and jQuery -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@@popperjs/core@2.5.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
