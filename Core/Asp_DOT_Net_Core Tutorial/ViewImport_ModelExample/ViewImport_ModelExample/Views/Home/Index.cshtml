@model List<Student> // here we not used namespace project and model we declare in viewimport , for global declare

@*@model ViewImport_ModelExample.Model.Student*@
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

<h3>Index Page</h3>

@{
    foreach (var item in Model)
    {
        <h4>@item.Id</h4>
        <h4>@item.Name</h4>
        <h4>@item.Age</h4>
        <h4>@item.City</h4>
        <h4>----------------</h4>
        <h4>----------------</h4>
    }
}
